name: Test

on:
  workflow_call:
  workflow_dispatch:
  
jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - run: mkdir dl
      - run: |
          set -x
          cd dl
          cd -
          pwd
      # - run: pwd
